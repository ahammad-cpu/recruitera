# Local editor / assistant config
.claude/
.vscode/
.idea/

# OS junk
.DS_Store
Thumbs.db

# Logs & temp
*.log
*.tmp
/tmp/

# Env / secrets (should never be committed)
.env
.env.*
*.pem

# recruitera-v2 is its own git repo (separate remote) — never nest it here
/recruitera-v2/
